<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar_collapse_section">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Epa Shopper Web', root_path, class: 'navbar-brand', id: 'logo' %>
    </div>

    <!-- COLLAPSIBLE PORTION OF NAV-BAR -->
    <!------------------------------------------------------------------------------>
    <div class="collapse navbar-collapse" id="navbar_collapse_section">

      <!-- LEFT ALIGNED CONTENT -->
      <!------------------------------------------------------------------------------>
      <ul class="nav navbar-nav">

        <li><%= link_to 'Shoper Reports', reports_path%></li>
        <li><%= link_to 'Product Change Requests', root_path%></li>

        <% if is_admin? %>
          <li><%= link_to 'Shoppers', shoppers_path%></li>
        <% end %>

      </ul>

      <!-- RIGHT ALIGNED CONTENT -->
      <!------------------------------------------------------------------------------>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li><%= link_to 'Logout', login_path, method: :delete %>
        <% else %>
            <li><%= link_to 'Admin', login_path, method: :get %></li>
        <% end %>

      </ul>

    </div>
  </div>
</nav>